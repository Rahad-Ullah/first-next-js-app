import Counter from "@/components/Counter/Counter";

const HomePage = () => {
  // throw new Error('')
  return (
    <div>
      <h2>Welcome to Next.js app</h2>
      <Counter></Counter>
    </div>
  );
};

export default HomePage;